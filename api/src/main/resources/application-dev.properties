spring.datasource.url=jdbc:postgresql://${DB_HOST:localhost}:5432/${DB_NAME:kiosk}
spring.datasource.username=${DB_USER:kiosk}
spring.datasource.password=${DB_PASSWORD:kiosk}
spring.jpa.hibernate.ddl-auto=create-drop
